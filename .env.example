# Storage Provider
# - localStorage (default): Browser storage for V1
# - api: REST API for V2/V3
VITE_STORAGE_PROVIDER=localStorage

# API URL (only used when VITE_STORAGE_PROVIDER=api)
# V2: Netlify Functions
VITE_API_URL=/.netlify/functions/annotations

# V3: AWS API Gateway (uncomment for production)
# VITE_API_URL=https://xxx.execute-api.region.amazonaws.com/prod/annotations
